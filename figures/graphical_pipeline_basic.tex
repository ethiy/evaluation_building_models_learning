\documentclass[tikz]{standalone}

\usepackage[T1]{fontenc}
\usepackage[english]{babel}

\usepackage{standalone}

\usetikzlibrary{3d}

\begin{document}
    \begin{tikzpicture}
    	\node[draw, very thick, rectangle, rounded corners, visible on=<2->] (residual) at (0,0) {\Large Features};
        
        \path (residual.north) + (0, 1.5) node[anchor=south, text width=2.5cm, align=center] (model_legend) {\footnotesize Input model};
        \path (model_legend.north) node[anchor=south] (model) {\includestandalone[mode=buildnew, height=1.5cm]{../figures/graphical_abstract/building_model}};

		\path (residual.south) + (0, -1.5) node[anchor=north, visible on=<2->] (errors) {\includestandalone[mode=buildnew, height=1.5cm]{../figures/graphical_abstract/building_errors}};
        \path (errors.south) node[anchor=north, text width=2.5cm, align=center, visible on=<2->] (errors_legend) {\footnotesize Error detection};
        \path (errors.15) + (.4, 0) node[align=center, visible on=<2->] (errors_list) {\footnotesize {\color{red} \(\blacksquare\) \texttt{\acrshort{acr::fib}}}};
        \path (errors_list.south) node[align=center, visible on=<2->] (errors_list) {\footnotesize {\color{green} \(\blacksquare\) \texttt{\acrshort{acr::fos}}}};

        \path[draw, ->, line width=2pt, rounded corners=10pt, IGNGreen, visible on=<2->] (model_legend.south) -- (residual.north);
        \path[draw, ->, line width=2pt, rounded corners=10pt, IGNGreen, visible on=<3->] (residual.south) -- (errors.north) node [midway, left, IGNGreen] {\footnotesize Supervised classifier (RF, SVM)};
    \end{tikzpicture}
\end{document}
