\documentclass[tikz]{standalone}

\usepackage[T1]{fontenc}
\usepackage[english]{babel}

\usepackage{tikz}
\usetikzlibrary{positioning, patterns, shapes.misc}

\usepackage[
    backend=biber,
    style=authoryear,
    dashed=false,
    sorting=nty,
    maxbibnames=5,
    minbibnames=5,
    maxcitenames=2,
    mincitenames=2,
    uniquelist=false,
    uniquename=false,
    hyperref=true,
    backref=true,
    backrefstyle=all+,
    isbn=false,
    url=false,
    doi=false
]{biblatex}
\addbibresource{references.bib}

\begin{document}
    \tikzset{
        data/.style={
            draw,
            thick,
            minimum height = .75cm,
            minimum width  = 8cm,
            fill           = yellow!30
        }
    }
    \tikzset{
        output/.style={
            draw,
            thick,
            minimum height = 5 cm,
            minimum width  = .75 cm,
            fill           = yellow!30
        }
    }
    \tikzset{
        intersection/.style={
            draw,
            thick,
            minimum height = 5 cm,
            minimum width  = 8 cm
        }
    }
    \tikzset{
        cross/.style={
            cross out,
            draw=black,
            minimum size=1.1*(#1-\pgflinewidth),
            inner sep=0pt,
            outer sep=0pt
        },
        cross/.default={5pt}
    }

	\begin{tikzpicture}
		\node[data]  (manual) {\textbf{Manual data}};
		\path (manual.east) node[data, anchor=west] (sensor) {\textbf{Sensor data}};
		\path (manual.north west) node[output, anchor=south east] (semantic_errors) {\rotatebox{90}{\textbf{Semantic errors}}};
		\path (semantic_errors.north) node[output, anchor=south] (geom_ind) {\rotatebox{90}{\textbf{Geometric indices}}};

        \path (manual.north) node[intersection, anchor=south] (manual_semantic) {};
		\path (manual_semantic.north) node[intersection, anchor=south] (manual_indx) {};
		\path (sensor.north) node[intersection, anchor=south] (sensor_semantic) {};
		\path (sensor_semantic.north) node[intersection, anchor=south] (sensor_indx) {};

	    \path (manual_indx) + (0, -.5) node[cross, label=below:{\scriptsize \parencite{zeng2014multicriteria}}]{};
	    \path (manual_indx) + (-1, 1) node[cross, label=above:{\scriptsize \parencite{vogtle2003quality}}]{};
	    \path (manual_indx) + (1.5, .5) node[cross, label=below:{\scriptsize \parencite{kaartinen2005accuracy}}]{};
	    \path (sensor_indx) node[cross, label=below:{\scriptsize \parencite{akca2010quality}}]{};
	    \path (sensor_semantic) + (0, 1.75) node[cross, label=below:{\scriptsize \parencite{elberink2011quality}}]{};
	    \path (sensor_semantic) + (-.75, .25) node[cross, label=below:{\scriptsize \parencite{boudet2006supervised}}]{};
	    \path (sensor_semantic) + (1.5, -.75) node[cross, label=below:{\scriptsize \parencite{michelin2012building}}]{};
	\end{tikzpicture}
\end{document}
