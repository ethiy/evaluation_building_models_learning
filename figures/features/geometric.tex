\documentclass[tikz]{standalone}

\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{fourier}

\usepackage{xcolor}
\definecolor{darkgreen}{RGB}{0, 155, 85}

\usepackage{standalone}

\usepackage{pgfplots}

\usepackage{tikz}
\usetikzlibrary{shapes, arrows, shadows, calc, decorations}

\usepackage{mathrsfs, amsmath}


\begin{document}
    \begin{tikzpicture}
    	\node (facet_graph) {\includestandalone[mode=tex, height=4cm]{figures/graphical_abstract/building_graph}};
        \path (facet_graph) + (-.2, -1) node (f) {\small $f$};
        \path (facet_graph) + (2, -.8) node (g) {\small $g$};
        
        \path (f) + (-2, -1) node[darkgreen] (facet_attributes) {\tiny
        $\begin{bmatrix}
        	d(f)\\
            \mathscr{A}(f)\\
            \mathscr{C}(f)
         \end{bmatrix}$
        };
        \path (f) + (1.5, -1.5) node[blue] (edge_attributes) {\tiny
        $\begin{bmatrix}
            \vert\vert \mathscr{G}(f)- \mathscr{G}(g) \vert\vert_2\\
            \arccos(\vec{n}(f) \cdot \vec{n}(g))
         \end{bmatrix}$
        };
        
        \path[draw, ->, line width=1pt, rounded corners=10pt, darkgreen] (f.185) -- (facet_attributes.north);
        \path (edge_attributes.north |- f.east) + (-.25, .4) node (edge) {};
        \path[draw, ->, line width=1pt, rounded corners=10pt, blue] (edge) -- (edge |- edge_attributes.north);
    \end{tikzpicture}
\end{document}
